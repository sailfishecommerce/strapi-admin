'use strict';

/**
 * page-feature-carousel service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::page-feature-carousel.page-feature-carousel');
